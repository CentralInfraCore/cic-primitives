project:
  name: cic-primitives
  description: CIC meta-schema layer — atomic and aggregate primitives
  license: CC-BY-NC-SA-4.0
  main_branch: primitives/main
  owner: Gabor Zoltan Sinko
  maintenance:
    status: active
    supported_until: '2027-12-31'
  contacts:
    - type: email
      value: gabor.sinko@paynance.hu
  links:
    - name: repository
      url: https://github.com/CentralInfraCore/cic-primitives.git

compiler_settings:
  meta_schemas_dir: ./
  source_dir: schemas
  meta_schema_file: md.meta.schema.yaml
  primitive_schema_file: schemas/index.yaml
  vault_key_name: cic-my-sign-key

x-cic:
  repo_type: primitive
  # repo_type: primitive — szándék dokumentálva.
  # A compiler_settings-be akkor kerül, ha a base-repo compiler.py támogatja.
